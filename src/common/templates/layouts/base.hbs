<html lang='pt-BR'>
  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>{{title}}</title>
    {{> styles}}
  </head>
  <body>
    <div class='container'>
      {{> header emoji=emoji}}

      <div class='content'>
        {{{body}}}
      </div>

      {{> footer year=year}}
    </div>
  </body>
</html>
