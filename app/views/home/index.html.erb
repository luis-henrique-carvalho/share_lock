<div class="max-w-4xl mx-auto p-8">
  <h1 class="text-3xl font-bold mb-6">Teste de Componentes RubyUI</h1>

  <%# Renderizando um componente de Alerta %>
  <%= render RubyUI::Alert.new(title: "Instalação Concluída!") do %>
    Se você está vendo este alerta, o RubyUI foi instalado com sucesso.
  <% end %>

  <div class="mt-8">
    <%# Renderizando um Botão Primário %>
    <%= render RubyUI::Button.new(variant: :primary) do %>
      Botão Primário
    <% end %>

    <%# Renderizando um Botão Secundário %>
    <%= render RubyUI::Button.new(variant: :secondary) do %>
      Botão Secundário
    <% end %>

    <%# Renderizando um Avatar %>
    <%= render RubyUI::Avatar.new(src: "https://avatars.githubusercontent.com/u/24623422?v=4") %>
  </div>

  <div class="mt-8">
    <%# Renderizando um Badge %>
    <%= render RubyUI::Badge.new(variant: :success) do %>
      Sucesso
    <% end %>
    <%= render RubyUI::Badge.new(variant: :destructive) do %>
      Erro
    <% end %>
  </div>
</div>
